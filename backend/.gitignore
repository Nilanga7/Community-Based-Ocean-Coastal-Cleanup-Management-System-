# Uploaded verification documents (local disk storage — see FileStorageService /
# VERIFICATION_DOCS_DIR in application.properties). Default location; if VERIFICATION_DOCS_DIR
# is overridden to point elsewhere, that path won't be covered by this line.
uploads/
